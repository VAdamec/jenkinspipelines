version: '2'
services:
  app:
    build: ./app/
    command: python app.py
    volumes:
      - ./app/sample:/app
    ports:
      - "15000:5000"
    links:
      - backend
  backend:
    build: ./backend
    command: redis-server
    volumes:
      - ./tmp/redis:/data
  tester:
    build: ./tester
    links:
      - backend
      - app
    volumes:
      - ./:/code
